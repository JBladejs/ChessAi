package com.bladejs.chess.misc

class IntColor(red: Int, green: Int, blue: Int, alpha: Int) {
    val red = red / 255f
    val green = green / 255f
    val blue = blue / 255f
    val alpha = alpha / 255f
}